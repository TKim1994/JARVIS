Para inicializar por primera vez:
git init : para crear la carpeta oculta ".git"
git remote add origin https://github.com/TKim1994/JARVIS.git

Para descargar:
git pull origin main

Para subir:
git commit -m "Comentario del commit"
git push -u origin HEAD:main

